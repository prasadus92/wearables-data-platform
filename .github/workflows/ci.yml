name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: timescale/timescaledb:latest-pg16
        env:
          POSTGRES_USER: wearables
          POSTGRES_PASSWORD: wearables
          POSTGRES_DB: wearables
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U wearables"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]

    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          uv venv
          uv pip install -e ".[dev]"

      - name: Lint (ruff)
        run: |
          .venv/bin/ruff check app tests
          .venv/bin/ruff format --check app tests

      - name: Tests
        run: .venv/bin/python -m pytest tests/ -v --cov=app --cov-report=term-missing
        env:
          DATABASE_URL: postgresql+asyncpg://wearables:wearables@localhost:5432/wearables_test
          REDIS_URL: redis://localhost:6379/1

      - name: Docker build
        run: docker build -t wearables-data-platform-backend .
